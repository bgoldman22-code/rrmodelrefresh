// README: This patch only affects serverless lock and does not change preview logic.
// If your UI shows "lock_picks failed: 500", deploy this function so the lock endpoint works with a minimal payload.
